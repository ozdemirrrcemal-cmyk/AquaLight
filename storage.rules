rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Firebase Storage is decommissioned for the app. Keep this deny-all policy deployed so
    // retired app versions cannot read, create, update or delete objects.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
