<?xml version="1.0" encoding="utf-8"?>
<ScrollView
  xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto"
  android:layout_width="match_parent"
  android:layout_height="match_parent"
  android:background="@color/background_color"
  android:fillViewport="true">

  <LinearLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="24dp"
    android:paddingEnd="24dp"
    android:paddingTop="28dp"
    android:paddingBottom="24dp">

    <!-- Açıklama -->
    <TextView
      android:id="@+id/tvStepDescription"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:layout_marginTop="10dp"
      android:text="@string/aquarium_text_add_a_visual_reference_for_this_aquarium_you_can_use_a_photo"
      android:textColor="#8FA4BE"
      android:textSize="14sp"
      android:lineSpacingExtra="3dp" />

    <!-- Fotoğraf kartı -->
    <com.google.android.material.card.MaterialCardView
      android:id="@+id/cardPhotoContainer"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:layout_marginTop="34dp"
      app:cardBackgroundColor="#10233A"
      app:cardCornerRadius="22dp"
      app:cardElevation="0dp"
      app:strokeColor="#223A57"
      app:strokeWidth="1dp">

      <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="18dp">

        <TextView
          android:id="@+id/tvInputTitle"
          android:layout_width="match_parent"
          android:layout_height="wrap_content"
          android:text="@string/aquarium_photo_title_full"
          android:textColor="?attr/colorOnSurface"
          android:textSize="16sp"
          android:textStyle="bold" />

        <TextView
          android:id="@+id/tvInputHint"
          android:layout_width="match_parent"
          android:layout_height="wrap_content"
          android:layout_marginTop="4dp"
          android:text="@string/aquarium_text_use_a_photo_to_make_this_aquarium_easier_to_recognize_later"
          android:textColor="#7F91AA"
          android:textSize="13sp"
          android:lineSpacingExtra="2dp" />

        <!-- Görsel alanı -->
        <com.google.android.material.card.MaterialCardView
          android:id="@+id/cardAquariumImage"
          android:layout_width="match_parent"
          android:layout_height="220dp"
          android:layout_marginTop="18dp"
          app:cardCornerRadius="18dp"
          app:cardElevation="0dp"
          app:strokeWidth="0dp">

          <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <ImageView
              android:id="@+id/imgAquariumPhoto"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:contentDescription="Aquarium photo"
              android:scaleType="centerCrop"
              android:src="@drawable/nature_aquarium" />

            <!-- Hafif koyu overlay -->
            <View
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:background="#26000000" />

            <!-- Sağ alt kamera butonu -->
            <com.google.android.material.card.MaterialCardView
              android:id="@+id/btnCamera"
              android:layout_width="44dp"
              android:layout_height="44dp"
              android:layout_gravity="bottom|end"
              android:layout_marginEnd="14dp"
              android:layout_marginBottom="14dp"
              android:clickable="true"
              android:focusable="true"
              android:foreground="?attr/selectableItemBackgroundBorderless"
              app:cardBackgroundColor="#800D1D31"
              app:cardCornerRadius="14dp"
              app:cardElevation="4dp"
              app:strokeColor="#33FFFFFF"
              app:strokeWidth="1dp">

              <ImageView
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:layout_gravity="center"
                android:contentDescription="Select photo"
                android:src="@drawable/ic_camera_24"
                app:tint="@android:color/white" />

            </com.google.android.material.card.MaterialCardView>

          </FrameLayout>

        </com.google.android.material.card.MaterialCardView>

        <!-- Add Plant butonu -->
        <com.google.android.material.button.MaterialButton
          android:id="@+id/btnAddPlant"
          style="@style/Widget.Aqua.PrimaryButton"
          android:layout_width="match_parent"
          android:layout_height="52dp"
          android:layout_marginTop="18dp"
          android:text="@string/aquarium_text_add_plant"
          android:textAllCaps="false"
          android:textSize="16sp"
          android:textStyle="bold" />
        <LinearLayout
          android:id="@+id/selectedPlantsContainer"
          android:layout_width="match_parent"
          android:layout_height="wrap_content"
          android:layout_marginTop="18dp"
          android:orientation="vertical" />
      </LinearLayout>

    </com.google.android.material.card.MaterialCardView>



  </LinearLayout>

</ScrollView>