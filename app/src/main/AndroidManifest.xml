<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- 🟢 SplashActivity: Uygulamanın başlangıç noktası -->
        <activity
            android:name="com.aqua.aqualight.ui.splash.SplashActivity"
            android:exported="true"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 🔵 MainLoginActivity: Login, Register, ResetPassword fragmentlarını içerir -->
        <activity
            android:name="com.aqua.aqualight.ui.main.MainLoginActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/AppTheme" />

        <!-- ⚫ MainActivity: Başarılı girişten sonra ana ekran -->
        <activity
            android:name="com.aqua.aqualight.ui.main.MainActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/AppTheme" />

    </application>
</manifest>